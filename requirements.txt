# Repo-wide coursework environment  -  Python 3.11  (see .python-version)
#
# Covers SC3102, SC2107 and general signal-processing / plotting work.
# Direct dependencies only, pinned. For an exact reproduction, install
# requirements.lock.txt instead.
#
# EXCEPTIONS - not installed here, they have their own files:
#   TinyML_SC4172/requirements.txt           Python 3.8, tensorflow 2.4.1 (Arduino-pinned)
#   20250513 python driver/requirements.txt  Raspberry Pi target hardware only

# --- Signal processing & math ---
numpy==2.4.6
scipy==1.17.1
pandas==3.0.6

# --- Plotting & imaging ---
matplotlib==3.11.2
opencv-python==5.0.0.93

# --- Notebooks ---
ipykernel==7.3.0
